/**
 * Defines a commitlint configuration by extending @commitlint/config-conventional.
 * These rules can be overriden in this file if needed.
 */
module.exports = {
    extends: ["@commitlint/config-conventional"]
};
